[build]
builder = "NIXPACKS"
buildCommand = "bundle install"

[deploy]
startCommand = "bundle exec rails -v"
healthcheckPath = "/health"

[environments.production]
variables = { RAILS_ENV = "production" }
