# Root Gemfile for Railway deployment
# This helps Nixpacks detect the project as Ruby/Rails
# The actual app is in the backend/ directory

source 'https://rubygems.org'
ruby '3.1.0'

# Minimal gemset for deployment detection
gem 'rails', '~> 7.0.0'
gem 'bundler'
